//求算两点间距离。wrong
/*
#pragma warning(disable:4996)//VS2022软件需要的特殊申明（给scanf的）
#include<stdio.h>
#include<math.h>
int main()
{
	int x1, y1, x2, y2;
	scanf("%d %d %d %d", &x1, &y1, &x2, &y2);
	x1 = pow(x1 - x2, 2); x2 = pow(y1 - y2, 2);
	printf("%d", sqrt(x1 + x2));
	return 0;
}*/
//验证算法*
/*#include<stdio.h>
#include<math.h>
int main()
{
	int a = pow(4, 2);
	int b = sqrt(9);
	printf(a, b);
	return 0;
}
*/
//两点间距离 应该✓的
/*#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    double x1, y1, x2, y2;
    double a;
    double b;
    double d;
    scanf("%lf%lf%lf%lf", &x1, &y1, &x2, &y2);
    
        a = x1 - x2;
        b = y1 - y2;
        d = sqrt(a * a + b * b);
        printf("%.2lf", d);
    
    return 0;
}*/
/*
验证算法*
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main(void)
{  
    double a = 0;
    scanf("%f", &a);
    printf("%f", a);
    return 0;
}
*/
/*
还是两点距离
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    double x1, y1, x2, y2;

    while (scanf("%lf%lf%lf%lf", &x1, &y1, &x2, &y2) != EOF)
    {
        x1 = x1 - x2;
        y1 = y1 - y2;
        x2 = sqrt(x1 * x1 + y1 * y1);
        printf("%.2lf", x2);
    }
    return 0;
}
*/
/*
运算球的体积
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
#define PI 3.1415927
int main()
{

    double a;
    while (scanf("%lf", &a) != EOF)
    {
        a = pow(a, 3);
        a = 4 * a * PI / 3;
        printf("%.3lf\n", a);
    }
    return 0;
}
*/
/*
验证算法*
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    double a;
    while (scanf("%lf", &a) != EOF)
    { 
        a = a*a;
        a = sqrt(a);
        printf("%.2lf\n", a); 
    }
    return 0;
}
*/
/*
成绩转换 if算法
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    float a;
    while (scanf("%f", &a) != EOF)
    {
        if (a <= 0 or a > 100)
            printf("Score is error!\n");
        else
        {
            if (90 <= a && a <= 100)                         
               printf("A\n");                                  
           if (80 <= a && a <= 89)                            
               printf("B\n");
            if (70 <= a && a <= 79)
               printf("C\n");
            if (60 <= a && a <= 69)
                 printf("D\n");
            if (0 <= a && a <= 59)
                printf("E\n");
        }
       
    }
    return 0;
}
*/

/*
求绝对值 简单算法
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    double a;
    while (scanf("%lf", &a) != EOF)
    {
        printf("%.2lf\n", fabs(a));
    }
    return 0;
}
*/
/*
成绩转换 Switch算法
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    int a;
    while (scanf("%d", &a) != EOF)
    {
        if (a >= 0)
        {
        a = a / 10;
        switch (a)
            {
            case 10:
            case 9:     printf("A\n"); break;
            case 8:     printf("B\n"); break;
            case 7:     printf("C\n"); break;
            case 6:     printf("D\n"); break;
            case 5:
            case 4:
            case 3:
            case 2:
            case 1:
            case 0:
                        printf("E\n"); break;
            default:   printf("Score is error!\n");
            }   
        }
        else
            printf("Score is error!\n");
    }
    return 0;
}
*/

/*
第几天？ Switch-case过程待改进
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    int a, b, c;
    while (scanf("%d/%d/%d", &a, &b, &c) != EOF)
    {
        if (a % 400 == 0&&b>2)
            a = 1;
        else
        {
            if (a % 100 == 0)   a = 0;
            else 
            {
                if (a % 4 == 0&&b>2)   a = 1; 
                else a = 0;
            }
        }
        switch (b)
        {
        case(1):b = 0; break;
        case(2):b = 31; break;
        case(3):b = 31+28; break;
        case(4):b = 31 + 28+31; break;
        case(5):b = 31 + 28 + 31+30; break;
        case(6):b = 31 + 28 + 31 + 30+31; break;
        case(7):b = 31 + 28 + 31 + 30 + 31+30; break;
        case(8):b = 31 + 28 + 31 + 30 + 31 + 30+31; break;
        case(9):b = 31 + 28 + 31 + 30 + 31 + 30 + 31+31; break;
        case(10):b = 31 + 28 + 31 + 30 + 31 + 30 + 31 + 31+30; break;
        case(11):b = 31 + 28 + 31 + 30 + 31 + 30 + 31 + 31 + 30+31; break;
        default:b = 31 + 28 + 31 + 30 + 31 + 30 + 31 + 31 + 30 + 31+30; break;
        }
        printf("%d\n", a + b + c);
    }
    return 0;
}
*/
/*
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    int i,n,x,j;
    while (scanf("%d", &n) != EOF)
    {
        j = 1;
        for (i = 1; i <= n; i++;)
        {
            scanf("%d", &x)；
/*直接x%2！=0也许可以？*/
if (x % 2 == 1 || x % 2 == -1)
                j *= x;
        }
        printf("%d\n", j);
    }
    return 0;
}
*/
/*
G 平方和与立方和
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    int m, n,a;
    while (scanf("%d %d", &n, &m) != EOF)
    {
        long long int b=0,c=0,x1,x2;
        if (n <= m) x1 = n, x2 = m;
        else x1 = m, x2 = n;
        for (a = x1; a <= x2; a++)
        {
            if (a % 2 == 0)  b += a * a;
    /*用else*/  if (a % 2 != 0) c += a * a * a;
        }
        printf("%lld %lld\n", b, c);
    }
    return 0;
}
*/
/*
统计 数据？正数 ，负数和零。
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    int  i,c=0,d=0,f=0;
    double a, b;
    while (scanf("%lf", &a) != EOF)
    {
        if (a == 0) break;
        else
        {
            for (i = 1; i <= a; i++)
            {
                scanf("%lf", &b);
                if (b < 0) c++;
                if (b == 0) f++;
                if (b > 0) d++;
            }
        }
        printf("%d %d %d\n", c, f, d);
        c = d = f = 0;
    }
    return 0;
}
*/

#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{

}
    return 0;
}
*/
/*
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    int m, n, i, a, b, c, d = 0;
    while (scanf("%d %d", &m, &n) != EOF)
    {
        for (i = m; i <= n; i++)
        {
            a = i / 100;
            b = i / 10 % 10;
            c = i % 10;
            if (i == a * a * a + b * b * b + c * c * c)
            {
                d++;
                printf("%d ", i);
            }
                
        }
        if (d != 0) printf("\n");
        if(d==0) printf("no\n");
        d = 0;
    }
    return 0;
}
*/
/*
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    int m,n,i;
    double a,b=0;
    while (scanf("%d", &m) != EOF)
    {
        for (i = 1; i <= m; i++)
        {
            scanf("%d", &n);
            if (n % 2 == 0) n = -n;\\\\\\\\\\\\\WRONG
            a = 1.0 / n;
            b += a;
            printf("%.2lf\n", b);
        }
        b = 0;
    }
    return 0;
}*/
/*
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    double a, b=0;
    scanf("%lf", &a);
    b = 1/a;
    printf("%.2lf\n", b);
    return 0;
}
*/
/*
#include <iostream>
#include <iomanip>
using namespace std;
int main()
{
    int n;
    float a = 1, sum = 0;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        sum += 1 / a; if (a > 0)
        {
            a = -(a + 1); continue;//////////////////////////////////////
        }
        if (a < 0)
        {
            a = -(a - 1);
            continue;
        }
        
    }
    cout << fixed << setprecision(2) << sum << endl;
}*/
/*
#pragma warning(disable:4996)
#include<cstdio>
#include<cmath>
 using namespace std;
int main()
{
         int n;
         while (scanf("%d", &n) != EOF)
         {
for (int i = 0; i < n; ++i)
             {
                 int m;
                 scanf("%d", &m);
                 double sum = 0.0;
                 for (int j = 1; j <= m; ++j)
                     {
                         sum += pow(-1, j + 1) / j;
                     }
                 printf("%.2lf\n", sum);
             }
         }
}*/
/*
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    int i, n, a, b=0, x, y;
    while (scanf("%d %d",&x,&y) != EOF)
    {
        if (x == 0 && y == 0)  break;
        if(x<-39||y>50||x>y) printf("Sorry\n");
        else for (i = x; i <= y; i++)
        {
            n = i * i + i + 41;
            for (a = 2; a < n; a++)
            {
                if (n % a == 0) b = 1;
            }
        }
        if (b == 0) printf("OK\n");
        else printf("Sorry\n");
        b = 0;
    }
    return 0;
}
*/
/*
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    int n, m;
    while (scanf("%d", &n) != EOF)
    {
        m = 1;
        for (int i = 1; i < n; i++)
        {
            m = (m + 1) * 2;
        }
        printf("%d\n", m);
    }
    return 0;
}
*/
/*
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    int n;
    double a,c=0,x=0,y=100;
    while (scanf("%d", &n) != EOF)
    {
        
            for (int i = 1; i <= n; i++)
            {
                scanf("%lf", &a);
                c += a;
                if (x < a) x = a;
                if (y > a) y = a;
            }
        c = c - x - y;
        c = c / (n - 2);
        printf("%.2lf\n", c);
        c = 0,x=0,y=100;
        
    }
    return 0;
}
*/
/*
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    int a;
    double n;
    while (scanf("%lf %d", &n,&a) != EOF)
    {
        n = n / (a - 2);
        printf("%.2lf\n", n);
    }
    return 0;
}
*/
/*
#pragma warning(disable:4996)
#include<stdio.h>
#include<math.h>
int main()
{
    int n, m, a, b, c;
    while (scanf("%d %d", &n, &m) != EOF)
    {
        a = n / m;
        b = n % m;
        for (int i = 0; i <a; i++)
        {
            c = (2 * (m + 1) + i * m * 4) / 2;
            printf("%d ", c);
        }
        if (b == 1) printf("%d\n", 2 * b + 2 * a * m);
        else if (b != 0)
            printf("%d\n", 2 * b + 2 + 2 * a * m);
        else printf("\n");
    }
    return 0;
}
*/

#include<stdio.h>
#include<math.h>
int main()
